# 🚀 إعداد بيئة تطوير لينُكس

هذا المستودع يحتوي على ثلاث سكربتات Bash مُصمَّمة لإعداد محطة عمل مطور بسرعة على توزيعات لينُكس الشائعة:

- `setup.sh` — Arch / Manjaro
- `ubuntu.sh` — Ubuntu / Debian
- `fedora.sh` — Fedora / RHEL

تقوم كل سكربتة بالتالي: تحديث النظام، تثبيت الأدوات الأساسية، تثبيت بيئات التطوير (Python, Node.js, Java, PHP، ...)، تثبيت وتكوين Docker، تثبيت تطبيقات سطح المكتب الاختيارية (VS Code, Postman, Steam، ...)، وإعداد Zsh + Oh My Zsh.

> اقرأ السكربتات قبل التشغيل. هذه السكربتات موجهة لأجهزة تطوير؛ اختبرها أولاً في آلة افتراضية قبل تشغيلها على نظام إنتاج.

---

## 📋 التوزيعات المدعومة

- Arch Linux / Manjaro → `setup.sh`
- Ubuntu / Debian → `ubuntu.sh`
- Fedora / RHEL → `fedora.sh`

---

## ✨ ماذا تفعل السكربتات (ملخص)

- تحديث نظام التشغيل والحزم
- تثبيت أدوات CLI الأساسية (git، curl، wget، vim، htop، ...)
- تثبيت أدوات تطوير:
  - أداة بناء C/C++ (gcc، make، cmake)
  - Python (python3، pip، venv)
  - Node.js + npm
  - Java (OpenJDK)
  - PHP + Composer
  - قواعد البيانات: MySQL / MariaDB
- تثبيت وتكوين Docker و Docker Compose
- تثبيت تطبيقات سطح المكتب (مثل VS Code، Postman، Steam، وحزم AUR على Arch)
- تثبيت وتكوين Zsh + Oh My Zsh + إضافات مقترحة وسمات
- تنظيف اختياري ونصائح ما بعد التثبيت (مثل تأمين MySQL/MariaDB)

---

## 🧩 الاستخدام السريع

من جذر المستودع:

اجعل السكربت قابلاً للتنفيذ (اختياري) ثم شغله:

```bash
# مثال: Ubuntu
chmod +x ./ubuntu.sh        # اختياري
bash ./ubuntu.sh

# مثال: Fedora
chmod +x ./fedora.sh
bash ./fedora.sh

# مثال: Arch/Manjaro
chmod +x ./setup.sh
bash ./setup.sh
```

يمكنك أيضاً تشغيل السكربت مباشرةً باستخدام bash دون تغيير الأذونات:
```bash
bash ./ubuntu.sh
```

مهم: شغّل هذه السكربتات كمستخدم يملك صلاحيات `sudo`.

---

## 🧭 سلوك كل سكربت (ماذا تتوقع)

### `setup.sh` (Arch / Manjaro)
- تحديث النظام عبر `pacman` (`sudo pacman -Syu`).
- تثبيت الحزم الأساسية وأدوات التطوير وتهيئة AUR عبر `yay` إذا لم يكن موجوداً.
- تثبيت: gcc, make, cmake, php, composer, python, jdk-openjdk, nodejs, npm, mariadb, docker, docker-compose.
- تثبيت تطبيقات سطح المكتب من AUR (مثل visual-studio-code-bin، postman-bin، steam).
- إعداد Zsh و Oh My Zsh وتعديل `~/.zshrc` لتفعيل سمات وإضافات.
- تفعيل خدمة Docker وإضافة المستخدم إلى مجموعة `docker`.

### `ubuntu.sh` (Ubuntu / Debian)
- تحديث الحزم: `sudo apt update && sudo apt upgrade -y`.
- تثبيت الحزم الأساسية وأدوات التطوير: build-essential، git، curl، python3، nodejs (عن طريق NodeSource عند الحاجة)، openjdk، mysql-server، docker.io.
- تفعيل Docker وإضافة المستخدم إلى مجموعة `docker`.
- تثبيت Zsh و Oh My Zsh (غير تفاعلي عندما يكون ممكناً).

### `fedora.sh` (Fedora / RHEL)
- تحديث النظام عبر `dnf` (`sudo dnf -y upgrade --refresh`).
- تثبيت الحزم الأساسية ومجموعة "Development Tools" وأدوات التطوير (gcc، make، cmake، python3، java-17-openjdk-devel، nodejs، npm).
- تثبيت Docker وتهيئته، وإضافة المستخدم إلى مجموعة `docker`.
- محاولة تثبيت Steam قد تتطلب تفعيل مستودعات طرف ثالث (RPM Fusion).

---

## 🧾 جدول — الأدوات والحزم (نظرة عامة)

| الفئة | أمثلة من الحزم |
|---|---|
| الأساسيات | git، curl، wget، unzip، zip، htop، neofetch، vim، nano |
| أدوات البناء | gcc، g++، make، cmake، base-devel (Arch)، Development Tools (Fedora) |
| اللغات | python3 (+pip، venv)، nodejs + npm، openjdk / jdk |
| بيئة PHP | php، composer |
| قواعد البيانات | mysql-server (Ubuntu/Fedora)، mariadb (Arch) |
| Docker | docker، docker-compose |
| تطبيقات سطح المكتب | VS Code (binary)، Postman، Steam |

> ملاحظة: أسماء الحزم قد تختلف حسب التوزيعة. راجع السكربتات للحصول على القوائم الدقيقة.

---

## ✅ المتطلبات المسبقة

- حساب مستخدم بصلاحيات `sudo`.
- اتصال بالإنترنت.
- يفضّل تشغيل السكربتات على جهاز اختبار (آلة افتراضية) قبل التشغيل على جهاز إنتاج.
- على Arch: توفر `git` (لاستنساخ `yay`).
- على Fedora/RHEL: قد تحتاج لتفعيل EPEL أو RPM Fusion لبعض الحزم.

---

## 🔧 خطوات ما بعد التثبيت وتوصيات

- مجموعة Docker
  - بعد `sudo usermod -aG docker $USER` ستحتاج لتسجيل الخروج ثم تسجيل الدخول مجدداً لتفعيل العضوية. بديل مؤقت:
    ```bash
    newgrp docker
    ```
- تأمين MariaDB / MySQL
  - شغّل:
    ```bash
    sudo mysql_secure_installation
    ```
  - واتبع التعليمات لتعيين كلمة مرور الروت وإزالة المستخدمين المجهولين.
- Zsh / Oh My Zsh
  - إذا رغبت بالعودة إلى bash:
    ```bash
    chsh -s /bin/bash
    ```
  - عدّل `~/.zshrc` لتخصيص السمات والإضافات.
- إعادة التشغيل
  - بعد تغييرات كبيرة أو بعد إضافة المستخدم لمجموعة docker يُنصح بإعادة التشغيل:
    ```bash
    sudo reboot
    ```
- التحقق من Docker
  ```bash
  sudo systemctl status docker
  docker run hello-world
  ```

---

## 🛠 نصائح استكشاف الأخطاء

- فشل تثبيت حزمة:
  - افحص الاتصال، أعد المحاولة، اطّلع على سجلات مدير الحزم (`/var/log/apt` أو `journalctl` أو إخراج `dnf`).
- فشل بناء حزمة AUR (على Arch):
  - تأكد من تثبيت `base-devel` والتبعيات اللازمة، وراجع مخرجات `makepkg`.
- فشل تثبيت Oh My Zsh:
  - تأكد من توفر `curl` والوصول للخارج عبر HTTPS.
- تطبيقات مثل Steam غير متاحة:
  - فعّل مستودعات الطرف الثالث المناسبة (multiverse، RPM Fusion).

---

## 🧭 أمثلة تشغيل

1) تجربة سريعة (Ubuntu)
```bash
# من جذر المستودع
bash ./ubuntu.sh
# نفّذ mysql_secure_installation ثم أعد التشغيل إذا طُلب.
```

2) Arch (مع AUR)
```bash
chmod +x ./setup.sh
bash ./setup.sh
# عند فشل حزم AUR أعد المحاولة يدوياً باستخدام yay.
```

3) Fedora
```bash
bash ./fedora.sh
# قد تحتاج لتفعيل مستودعات إضافية لإكمال بعض الحزم.
```

---

## 🤝 المساهمة

مساهمة مرحب بها — خطوات بسيطة:

1. فرّق المستودع (Fork)
2. أنشئ فرعاً: `git checkout -b feat/your-change`
3. اختبر التغييرات محلياً (يفضل في VM)
4. ارفع فرعك وافتح Pull Request

يرجى:
- جعل السكربتات قابلة للتشغيل مرات متعددة (idempotent) إن أمكن
- تفضيل الأعلام غير التفاعلية للتركيبات غير التفاعلية، أو جعل الخطوات التفاعلية اختيارية
- توثيق أي مستودعات أو حزم جديدة تُضاف

---

## 📝 الرخصة

هذا المستودع مرخّص بموجب MIT License. راجع ملف `LICENSE` للمزيد.

---

## 👨‍💻 المؤلف

**Zeini Cheikh**
[![GitHub](https://img.shields.io/badge/GitHub-Zeini--23025-black?style=flat&logo=github)](https://github.com/Zeini-23025)
<br>


## ℹ️ ملاحظات ختامية

- تقوم هذه السكربتات بتغييرات على مستوى النظام؛ اقرأها دائماً قبل التشغيل.
- استخدم آلة افتراضية للاختبار في البداية.


## ⭐ **لا تتردد في إعطاء نجمة إذا ساعدك هذا المشروع!**
